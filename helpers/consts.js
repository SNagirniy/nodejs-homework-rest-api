const path = require("path");
const TEMP = path.join(__dirname, "../tmp");
const AVATARS = "avatars";
const PUBLIC_DIR = path.join(__dirname, "../public");

module.exports = { TEMP, AVATARS, PUBLIC_DIR };
